confession-page（最大父容器）
├─ right-image-grid（右侧2×2图片网格）
│  └─ 4个grid-item（每个图片项，子元素）
│     └─ img（图片，grid-item的子元素）
├─ petal（漂浮花瓣，循环生成多个）
├─ page-header（顶部标题）
│  ├─ cute-title（主标题）
│  │  └─ heart-icon（爱心图标，子元素）
│  └─ sub-title（副标题）
├─ main-content（主体内容区，核心父容器）
│  ├─ avatar-box（头像框）
│  │  ├─ avatar-frame（头像框架）
│  │  │  ├─ avatar-img（头像图片）
│  │  │  ├─ video（视频元素，头像框架的子元素）
│  │  │  └─ avatar-decoration（装饰✨）
│  │  └─ avatar-text（头像下方文字）
│  ├─ love-card（手写卡片）
│  │  ├─ card-header（卡片标题）
│  │  │  └─ card-icon（卡片图标💌）
│  │  ├─ card-content（卡片内容，情书文字）
│  │  │  └─ 多个p标签（每一行文字，子元素）
│  │  └─ card-footer（卡片底部，日期）
│  ├─ button-group（互动按钮区）
│  │  └─ 3个cute-btn（按钮，子元素）
│  │     └─ span（按钮文字，按钮的子元素）
│  ├─ heart-container（爱心特效容器）
│  └─ quote-box（可爱语录区）
│     ├─ quote-content（语录内容）
│     └─ change-quote-btn（换语录按钮）
├─ page-footer（底部区域）
│  ├─ music-player（音乐播放器）
│  │  ├─ audio（音频播放器）
│  │  └─ music-text（音乐说明文字）
│  └─ copyright（版权信息）
└─ popup（弹窗）
   └─ popup-content（弹窗内容）
      ├─ popup-icon（弹窗图标）
      ├─ popup-text（弹窗文字）
      └─ popup-close（弹窗关闭按钮）



      在六七年前，一个















